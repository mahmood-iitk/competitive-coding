// Problem
Madoka is a very strange girl, and therefore she suddenly wondered how many pairs of integers (𝑎,𝑏) exist, where 1≤𝑎,𝑏≤𝑛, for which lcm(𝑎,𝑏)gcd(𝑎,𝑏)≤3.

In this problem, gcd(𝑎,𝑏) denotes the greatest common divisor of the numbers 𝑎 and 𝑏, and lcm(𝑎,𝑏) denotes the smallest common multiple of the numbers 𝑎 and 𝑏.

//Input
The input consists of multiple test cases. The first line contains a single integer 𝑡(1≤𝑡≤104) — the number of test cases. Description of the test cases follows.

The first and the only line of each test case contains the integer 𝑛(1≤𝑛≤108).

// Output

For each test case output a single integer — the number of pairs of integers satisfying the condition.

// Example
// Input
6
1
2
3
4
5
100000000

// Output
1
4
7
10
11
266666666

// Solution
#include <bits/stdc++.h>

using namespace std;

int main() {
  int t;
  cin >> t;
  while (t--) {
    int n;
    cin >> n;
    
    cout << 2 * (n/3) + 2 * (n/2) + n << endl;
  }
}
